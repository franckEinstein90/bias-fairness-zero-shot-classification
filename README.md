Bias & Fairness — Zero-Shot Classification

Goal:
- Use an LLM as a classifier (toxic / non-toxic)
- Compute group-level fairness metrics (SPD, etc.)
- Keep implementation minimal and understandable

Scope:
- No integrated gradients (separate repo later)
- No heavy notebooks
- Script-based execution only